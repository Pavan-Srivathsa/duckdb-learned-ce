# Artifacts

Store exported ONNX models and training outputs here.

- `model.onnx` — exported regressor (generated, not committed by default)
- `model_metadata.json` — feature schema and training provenance
- `metrics.json` — validation metrics from training
